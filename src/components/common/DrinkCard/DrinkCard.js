import React from 'react';
import Card from '@material-ui/core/Card';
import Typography from '@material-ui/core/Typography';

export default function DrinkCard({drink}) {
  return (
    <Card>
      <Typography> This is the drink card</Typography>
    </Card>
  );
}
